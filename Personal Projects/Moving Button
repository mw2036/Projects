<!DOCTYPE html>
<html>
    <head>
        <style>
            body {
                background-color: black;
                margin: 0%;
            }

            #movingButton {
                padding: 2em 5em;
                position: relative;
                top: 45%;
                left: 45%;
            }

            #container {
                height: 100vh;
                width: 100vw;
            }
            
        </style>
    </head>
    <body>
        <div id="container">
            <button id="movingButton" onmouseenter="change()">Click me!</button>
        </div>
        <script>

            const winWidth = window.innerWidth,
            winHeight = window.innerHeight,
            b = document.getElementById("movingButton");

            function change()
            {
                let newTop = Math.floor(Math.random() * (winHeight - 500)) + 400;
                let newLeft = Math.floor(Math.random() * (winWidth - 500)) + 400;
                b.style.top = newTop + "px";
                b.style.left = newLeft + "px";
            }
        </script>
    </body>
</html>
